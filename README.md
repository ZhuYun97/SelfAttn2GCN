# SelfAttn2GCN

## Model
All GNN models can be used as classifier

## Parser
You can use `parser.py` to transform attention scores into adjcent matrices according to your threshold. Besides, you should input the representations of tokens, attention scores and padding masks, the function will outcome the batch type of data(constructed graphs).
